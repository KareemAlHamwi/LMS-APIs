info:
  name: My Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/user/me"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
