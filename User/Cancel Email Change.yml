info:
  name: Cancel Email Change
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/user/cancel-email-change
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
