info:
  name: Delete Book
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/api/books/1"
  headers:
    - name: Accept
      value: application/json
    - name: Accept-Language
      value: ar
  body:
    type: multipart-form
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
