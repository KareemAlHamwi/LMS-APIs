info:
  name: Withdraw
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/wallet/admin-withdraw"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
