info:
  name: Withdraw
  type: http
  seq: 5

http:
  method: POST
  url: http://127.0.0.1:8000/api/wallet/admin-withdraw
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
