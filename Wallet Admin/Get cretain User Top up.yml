info:
  name: "Get cretain User Top up "
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/wallet/admin/2"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
