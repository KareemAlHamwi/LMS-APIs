info:
  name: Get All User Wallets
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/wallet/admin/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
