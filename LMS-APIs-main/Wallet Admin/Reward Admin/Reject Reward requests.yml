info:
  name: Reject Reward requests
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8000/api/reward/admin/reject/3
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
